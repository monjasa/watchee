import { BackdropUrlPipe } from './backdrop-url.pipe';

describe('BackdropUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new BackdropUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
